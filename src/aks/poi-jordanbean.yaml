apiVersion: apps/v1
kind: Deployment
metadata:
  name: tripinsights-backend-poi-deployment
  namespace: tripinsights-backend
  labels:
    app: tripinsights-backend-poi
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tripinsights-backend-poi
  template:
    metadata:
      labels:
        app: tripinsights-backend-poi
    spec:
      containers:
      - name: poi
        image: registryltt9233.azurecr.io/jordanbean/poi:1.0
        ports:
        - containerPort: 80
        env:
        - name: SQL_USER
          valueFrom:
            secretKeyRef:
              name: sql-creds
              key: sql-user
        - name: SQL_PASSWORD
          valueFrom:
            secretKeyRef:
              name: sql-creds
              key: sql-password
        - name: SQL_SERVER
          valueFrom:
            secretKeyRef:
              name: sql-creds
              key: sql-server
        - name: SQL_DBNAME
          valueFrom:
            secretKeyRef:
              name: sql-creds
              key: sql-dbname
        - name: CREDENTIAL_METHOD
          value: username_and_password

---

apiVersion: v1
kind: Service
metadata:
  name: tripinsights-backend-poi-service
  namespace: tripinsights-backend
spec:
  selector:
    app: tripinsights-backend-poi
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80